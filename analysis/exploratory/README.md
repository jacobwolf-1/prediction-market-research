This directory contains retained research scripts that are useful for inspection or follow-up work, but are not part of the documented public reproduction path.

Canonical pipeline entrypoints live under `pipeline/`, `data_collection/`, and the non-exploratory scripts in `analysis/`.
